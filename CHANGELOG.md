# Change Log

## 2026-08-04

### Added

- 新增项目级长期开发规范 `AGENTS.md`。
- 新增追加式项目变更记录文件。

### Changed

- 在 README 中补充开发与维护文档入口。

### Fixed

- 无。

### Removed

- 无。

## 2026-08-04

### Added

- 新增学习日期选择记忆功能。
- 新增每日词库单词删除功能和删除确认。
- 新增浮动消息、卡片入场和悬停动画。

### Changed

- 每日词库改为响应式卡片网格展示。
- 优化词库未找到单词时的提示文案。

### Fixed

- 修复连续添加单词时需要重复选择日期的问题。

### Removed

- 无。

## 2026-08-04

### Added

- 无。

### Changed

- 每日词库恢复为简洁的纵向词条展示。
- 每日词库列表仅展示单词和释义，词组仍保留在背诵模式中。

### Fixed

- 无。

### Removed

- 移除每日词库的卡片入场、悬停动画和词组展示。

## 2026-08-04

### Added

- 新增独立日期词库页面，以折叠方式查看全部日期。
- 新增连续录词焦点保持功能，点击录词区域外可退出。

### Changed

- 主界面改为只展示当前选择日期的单词。
- 日期切换移动到日期词库页面，切换后返回主界面添加或背诵。

### Fixed

- 避免历史日期和单词持续拉长主界面。

### Removed

- 移除主界面的日期下拉选择器和全部日期同时展示。

## 2026-08-04

### Added

- 新增独立间隔重复模块，包含熟练度、加权随机和复习间隔规则。
- 新增 1 至 5 级熟练度、答对答错次数及复习日期字段。
- 新增“不会 / 模糊 / 认识 / 熟练”四档复习操作。
- 新增熟练度、今日作答、待复习数量和进度展示。
- 新增权重计算、更新规则、旧词到期筛选和防连续重复测试。

### Changed

- 背诵模式改为抽取当前日期新词与已到期旧词。
- 学习记录将四档结果兼容映射到原有认识/不认识记录。
- README 增加间隔重复规则说明。

### Fixed

- 多词候选池不再连续抽到同一个单词。
- 答错单词保留在当天队列，并提高后续抽取权重。

### Removed

- 移除原有完全随机抽词和两按钮复习流程。

## 2026-08-04

### Added

- 新增设置页面和当前词书状态展示。
- 新增 `books` 词书目录与本地 PDF 自动发现。
- 新增词书选择持久化配置，切换后无需重启。
- 新增全部复习进度重置功能和二次确认。
- 新增词书管理与设置页面基础测试。

### Changed

- PDF 查询器根据当前选择自动切换词书。
- README 增加更换词书和重置进度说明。
- Git 忽略规则新增所有 PDF 文件。

### Fixed

- 多本 PDF 同时存在时不再因为无法确定词书而中断查询。

### Removed

- 无。

## 2026-08-04

### Added

- 新增 Windows 一键启动脚本 `start.bat`，可自动准备运行环境、启动服务并打开浏览器。

### Changed

- README 增加双击启动和停止应用的说明。

### Fixed

- 无。

### Removed

- 无。

## 2026-08-04

### Added

- 无。

### Changed

- 将当前 `六级词汇闪过.pdf` 从项目根目录移动到 `books` 文件夹统一管理。
- README 补充当前词书存放位置说明。

### Fixed

- 当前词书选择配置已同步为移动后的 `books` 路径。
- PDF 查找兼容项目根目录和 `books` 文件夹，避免移动词书后旧工具函数无法找到文件。

### Removed

- 无。

## 2026-08-04

### Added

- 设置页新增原始绿、海湾蓝、薰衣紫和柔粉四套主题颜色。
- 新增浏览器本地主题记忆，主题可在所有页面保持一致。
- 新增日期提示与主题选择的基础页面测试。

### Changed

- 空日期控件改为显示“请选择日期”，选择后继续显示实际日期。
- 页面主要强调色、柔和背景和装饰渐变统一使用主题变量。
- README 增加主题颜色说明。

### Fixed

- 无。

### Removed

- 无。

## 2026-08-04

### Added

- README 新增日常 GitHub 同步流程和本地文件忽略说明。
- 在项目外创建清理前的完整 Git 历史与 SQLite 数据备份。

### Changed

- 重写本地 Git 历史，仅保留程序代码和文档。
- 配置清理后的 `main` 用于后续常规快速推送。

### Fixed

- 修复历史提交包含超大 PDF、导致 GitHub 拒绝推送的问题。

### Removed

- 从所有 Git 历史提交中移除 PDF 词书；本地 `books` 中的词书保持不变。

## 2026-08-04

### Added

- 新增 Windows 一键启动脚本 `start_app.bat`。
- 新增本地服务健康检查和浏览器就绪等待模块。
- 新增桌面 `TOOLS/English Vocabulary` 快捷方式。
- 新增单实例、localhost 和浏览器启动基础测试。

### Changed

- Flask 改为仅监听 `127.0.0.1:5000`，不再开放局域网访问。
- 原有 `start.bat` 改为兼容入口，统一调用 `start_app.bat`。
- README 更新桌面启动、错误处理和关闭应用说明。

### Fixed

- 重复点击启动入口时不再创建多个 Flask 实例。
- 移除隐藏 PowerShell 浏览器启动方式，减少不必要的防火墙或安全软件提示。

### Removed

- 移除 `0.0.0.0` 监听和手机局域网访问说明。

## 2026-08-04

### Added

- 新增基于 Tailscale Funnel 的免费 HTTPS 手机远程访问配置脚本。
- 新增图形化访问密码初始化与更换、加盐哈希校验和 30 天登录保持功能。
- 新增远程地址查看与公网入口关闭脚本。
- 新增远程登录、安全跳转、Funnel 配置和移动端布局测试。

### Changed

- 优化窄屏页面间距、背诵卡片、统计区域和答题按钮的触控体验。
- README 增加无需手机 App 的安装、访问、关闭和隐私说明。

### Fixed

- 为公网访问增加统一登录保护，避免仅凭远程网址直接查看个人词库。

### Removed

- 无。

## 2026-08-04

### Added

- 新增 PDF 词头 OCR 容错测试，覆盖分隔符、缺失字母标记和少量错字。

### Changed

- PDF 查询会在严格词头定位的基础上兼容 `_`、`~`、`:` 等 OCR 异常字符。
- 快速筛页同时检查移除空格与 OCR 符号后的文本，减少异常页脚导致的漏词。
- 释义改为使用 PDF 中的实际词头文本确定截取起点。

### Fixed

- 修复 `atmosphere`、`inspire`、`promise`、`conquer` 等词头被 PDF 隐藏文本层错误识别后无法查询的问题。

### Removed

- 无。

## 2026-08-04

### Added

- 新增 `catalog`、`category` 词条边界与短词错误匹配回归测试。

### Changed

- 词头定位改为结合显示文字高度、音标相对高度和横向位置过滤正文碎片。
- 模糊 OCR 匹配只在页面底部词表确认存在查询词时启用。
- 统一词头识别与释义提取的纵向容差。

### Fixed

- 修复 `catalogue` 的正文碎片 `talo` 被误判为下一词头、导致 `catalog` 没有释义的问题。
- 修复 `category` 音标纵向偏移后只提取到下划线的问题。
- 修复损坏的 `genius` 词头可能被错误匹配成短词 `use` 的问题。

### Removed

- 无。

## 2026-08-04

### Added

- 新增 PDF 分裂词头回归测试，覆盖 `junior` 的 `jun` / `ior` 文本块结构。
- 新增整本词书分裂词头扫描与代表性实际查询验证。

### Changed

- 词头识别会受限合并同一 PDF 文本行中间距极小的连续英文片段。
- 释义改为从合并后词头的右侧坐标开始提取，避免分裂词头残留在释义中。
- README 增加分裂词头容错说明。

### Fixed

- 修复 `junior` 因隐藏文本被拆成 `jun` 和 `ior` 而无法录入的问题。
- 同步修复 `ingredient`、`convenient`、`genuine`、`paradise`、`premise`、`restrain`、`extraordinary`、`admire`、`diamond` 等同类漏词。

### Removed

- 无。

## 2026-08-04

### Added

- 背诵页新增熟悉程度待确认状态和“确认并进入下一个”操作。
- 新增先选熟悉程度、自动显示答案且允许改选的交互回归测试。

### Changed

- “不会 / 模糊 / 认识 / 熟练”按钮进入背诵页后即可直接选择，不再要求先点击显示释义。
- 选择熟悉程度后自动显示释义和词组；最终确认前可以切换其他选项。
- 只有点击确认按钮后才保存学习记录并抽取下一个单词。

### Fixed

- 修复背诵时必须先展开释义才能标记熟悉程度的操作限制。

### Removed

- 无。

## 2026-08-04

### Added

- 新增背诵页紧凑布局回归测试。

### Changed

- 背诵页内容区域由 760px 加宽至 860px，减少释义和词组换行。
- 缩小背诵页顶部留白、标题、统计卡片、单词卡片和操作区的纵向间距。
- 缩小桌面端单词标题和按钮高度，同时保留手机端触控按钮尺寸。

### Fixed

- 减少展开常见长度释义后需要频繁滚动页面的情况。

### Removed

- 无。
