# 项目开发规范

以下规范默认长期适用于本项目。除非用户明确提出新的要求，否则所有开发任务均须遵守。

## 开发原则

- 优先保证代码可读性、稳定性和可维护性，不以牺牲可读性来减少代码。
- 保持项目结构清晰，不增加无意义文件。
- 尽量采用模块化设计，避免把所有代码集中在一个文件中。
- 每完成一个阶段，都要确认项目能够正常运行。

## 修改原则

- 不一次性重构整个项目。
- 每次只完成一个明确功能，并保证功能可运行、不破坏已有功能、尽量减少无关修改。
- 修改已有代码时保留原有逻辑，只调整必要部分。

## 开发流程

每次开发均按以下流程执行，不得省略：

1. 分析需求。
2. 制定实现方案。
3. 修改代码。
4. 自检明显问题并验证项目可运行。
5. 按需同步更新 `README.md`。
6. 以追加方式更新 `CHANGELOG.md`，不删除历史记录。
7. 在最终回复中输出规定格式的 Change Log。
8. 给出符合 Conventional Commits 的建议提交信息。

## 风险处理

如果修改可能删除已有功能、修改数据库结构、删除文件、覆盖大量代码或调整项目架构，必须先停止修改并向用户说明：

- 为什么需要这样做。
- 存在哪些风险。
- 推荐采用什么方案。

取得用户确认后才能继续。

## 代码质量

- 函数职责单一，命名清晰。
- 只添加必要注释，避免重复代码。
- 保持目录整洁，不保留废弃代码或生成无用文件。
- 默认按长期维护项目设计，考虑后续阅读、维护和扩展成本。
- 如果存在明显更优但会扩大修改范围的方案，先提出建议，等待用户决定。

## 文档维护

- 功能或项目结构发生变化时同步维护 `README.md`。
- `CHANGELOG.md` 按日期追加记录，使用 `Added`、`Changed`、`Fixed`、`Removed` 分类。
- 没有内容的分类也应保留，并填写“无”。

## Git 与 GitHub 提交规范

- 所有新提交必须遵循 Conventional Commits，格式为 `type(scope): description`；不需要 scope 时使用 `type: description`。
- 根据实际改动选择准确类型，常用类型包括 `feat`、`fix`、`style`、`docs`、`refactor`、`test`、`chore`、`build` 和 `ci`。
- 提交说明应简洁描述实际变化，不使用 `fix 1`、`update`、`change` 等无法辨认用途的占位名称。
- 提交或推送前先检查实际差异、测试结果和目标分支，避免把本地数据库、PDF、密钥或其他个人数据加入 Git。
- 向 GitHub 推送后，向用户报告提交说明、提交哈希和同步结果。
- 如果操作需要用户登录、授权、处理冲突、选择目标分支或确认强制推送，必须先明确说明用户需要完成的步骤；未经明确确认不得执行高风险 Git 操作。

## 最终回复格式

每次任务完成后，回复末尾必须包含以下完整结构：

```text
Change Log

Added（新增）
...

Changed（修改）
...

Fixed（修复）
...

Removed（删除）
...

Files（影响文件）
...

Database
是否修改数据库：是 / 否
如有修改，说明具体内容。

Dependencies
是否新增依赖：是 / 否
如有新增，说明安装命令。

Need to Run
完成后是否需要重新运行项目：是 / 否
如需运行，给出完整命令。

Need to Retrain
是否需要重新训练模型：是 / 否
如需要，说明原因。

Suggested Git Commit
符合 Conventional Commits 规范的 Commit Message
```
